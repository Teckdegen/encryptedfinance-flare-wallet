# encryptedfinance-flare-wallet

Privacy-preserving DeFi wallet UI for Encrypted Finance on Flare Coston2 (TEE-backed).
Next.js app — needs no .env; endpoints are baked into app/lib/contracts.ts.

```bash
npm install
npm run dev
```
